{
  "name": "neondeck-windows-daemon",
  "version": "0.1.0",
  "description": "Windows-compatible, self-contained build of the NeonDeck backend daemon (WebSocket + HTTP API, code-execution engine, DeepSeek AI logic, Stripe checkout + webhooks). The @ide/shared protocol/tiers are vendored under src/shared so this folder builds and runs with no monorepo workspace.",
  "type": "module",
  "bin": {
    "neondeck-daemon": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "http-proxy": "^1.18.1",
    "localtunnel": "^2.0.2",
    "openai": "^4.56.0",
    "stripe": "^17.7.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/http-proxy": "^1.17.14",
    "@types/node": "^20.14.0",
    "@types/ws": "^8.5.10",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
